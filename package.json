{
  "name": "smart-hub",
  "description": "Контентный хаб Умного Дома с Алисой: сценарии автоматизации и руководства",
  "author": "illia",
  "license": "MIT",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist && node scripts/css-preload.js",
    "postbuild": "node scripts/indexnow.js",
    "preview": "astro preview",
    "astro": "astro",
    "pretest": "npm run lint && npm run typecheck && npm run build",
    "test": "node --test tests/*.test.*",
    "typecheck": "astro check",
    "lint": "npx @biomejs/biome check src/ tests/",
    "format": "npx @biomejs/biome format --write src/ tests/",
    "check": "npm run lint && npm run typecheck"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^6.4.7"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "pagefind": "^1.5.2",
    "typescript": "^6.0.3"
  }
}
